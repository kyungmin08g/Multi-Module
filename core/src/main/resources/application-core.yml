server:
  port: 8080

spring:
  profiles:
    name: production

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PW}

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update

name: 안녕1